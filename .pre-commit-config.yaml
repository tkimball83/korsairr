---
default_stages:
  - pre-commit

repos:
  - repo: https://github.com/psf/black-pre-commit-mirror
    rev: 26.5.1
    hooks:
      - id: black
        exclude: ^venv/
        files: \.(py)$
        verbose: true
...
