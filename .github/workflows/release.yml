---
name: release
'on':
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: korsairr:smoke
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker run --rm --user root --entrypoint python korsairr:smoke -m compileall -q .
      - run: docker run --rm --entrypoint python korsairr:smoke -c 'import korsairr'
      - run: |
          docker run --detach --name smoke korsairr:smoke
          sleep 5
          docker logs smoke | grep -q 'No swabbers enabled'
          docker rm --force smoke
  release:
    if: startsWith(github.ref, 'refs/tags/')
    name: release
    needs: test
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4.5.2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/tkimball83/korsairr
          tags: |
            type=ref,event=tag
            type=raw,value=latest
      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
...
